import React from 'react';
import EmptyMaze from './EmptyMaze';

const Main = (props) => {
    return (
        <div className="main_area">
            <EmptyMaze />
        </div>
    );
};

export default Main;